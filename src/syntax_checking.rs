pub struct SyntaxChecking {}

impl SyntaxChecking {
    pub fn check(&self, code_to_check:&String){
        let list_of_chars = code_to_check.as_bytes();
        // Todo : Has to check if the number of [ it's the same as ]
    }
}